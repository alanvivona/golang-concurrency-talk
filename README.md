# golang-concurrency-talk


```bash
make docker
```
By default this will scrap the test site inside `/test-site`.  
Change `ROOT` and `DEPTH` env vars to target other sites and change the max depth.  